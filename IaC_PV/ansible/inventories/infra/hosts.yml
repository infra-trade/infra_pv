all:
  hosts:
    pv-infra-master:
      ansible_host: 192.168.5.85
      ansible_user: pvinfra

    pv-infra-nodo1:
      ansible_host: 192.168.5.86
      ansible_user: pvinfra

    pv-infra-nodo2:
      ansible_host: 192.168.5.87
      ansible_user: pvinfra

    localhost:
      ansible_connection: local

  children:
    # Hosts donde gestionamos Docker
    docker_hosts:
      hosts:
        pv-infra-master:
        pv-infra-nodo1:
        pv-infra-nodo2:

    # Nodo donde corre Portainer Server
    portainer_server:
      hosts:
        pv-infra-master:

    # Nodos donde corren Portainer Agent
    portainer_agents:
      hosts:
        pv-infra-nodo1:
        pv-infra-nodo2:

    # Grupo usado para cargar ansible/group_vars/vcenter.yml
    # y ejecutar el play 20_vcenter_deploy_ubuntu_infra.yml
    vcenter:
      hosts:
        localhost: