---
- name: Crear VM base ubuntu-seed-24-04 con ISOs para autoinstall
  hosts: localhost
  gather_facts: false

  vars:
    # Ruta del venv para módulos VMware
    venv_path: "{{ lookup('env','HOME') + '/.ansible/venvs/vmware' }}"

  pre_tasks:
    ########################################################################
    # 1) Preparar venv con pyvmomi/requests
    ########################################################################
    - name: Crear venv para módulos VMware (si no existe)
      become: true
      ansible.builtin.shell: |
        set -e
        VENV="{{ venv_path }}"
        if [ ! -x "$VENV/bin/python" ]; then
          mkdir -p "$(dirname "$VENV")"
          python3 -m venv "$VENV"
          "$VENV/bin/pip" install --upgrade pip
          "$VENV/bin/pip" install pyvmomi requests
        fi
      args:
        executable: /bin/sh
      changed_when: false

    - name: Usar Python del venv
      ansible.builtin.set_fact:
        ansible_python_interpreter: "{{ venv_path }}/bin/python"

    ########################################################################
    # 2) Cargar group_vars/all.yml y vault.yml
    ########################################################################
    - name: Cargar group_vars/all.yml si existe
      ansible.builtin.stat:
        path: "{{ playbook_dir }}/../group_vars/all.yml"
      register: all_stat

    - name: include all.yml
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/../group_vars/all.yml"
      when: all_stat.stat.exists

    - name: Cargar vault.yml si existe
      ansible.builtin.stat:
        path: "{{ playbook_dir }}/../vault.yml"
      register: vault_stat

    - name: include vault.yml
      ansible.builtin.include_vars:
        file: "{{ playbook_dir }}/../vault.yml"
      when: vault_stat.stat.exists
      no_log: true

    ########################################################################
    # 3) Resolver parámetros vCenter
    ########################################################################
    - name: Resolver parámetros vCenter
      ansible.builtin.set_fact:
        vcenter_hostname: "{{ vcenter_hostname | default(lookup('env','VCENTER_HOSTNAME')) }}"
        vcenter_username: "{{ vcenter_username | default(lookup('env','VCENTER_USERNAME')) }}"
        vcenter_password: "{{ vcenter_password | default(lookup('env','VCENTER_PASSWORD')) }}"
        vcenter_datacenter: "{{ vcenter_datacenter | default('') }}"
        vcenter_datastore: "{{ vcenter_datastore | default('') }}"
        vcenter_cluster: "{{ vcenter_cluster | default('') }}"
        vcenter_network: "{{ vcenter_network | default('') }}"
        vcenter_validate_certs: "{{ vcenter_validate_certs | default(false) }}"
        vcenter_port: "{{ (vcenter_port | default(443)) | int }}"
        datastore_iso_dir: "{{ datastore_iso_dir | default('ISOS') }}"

    ########################################################################
    # 4) Definir nombres/rutas de ISOs (sin descarga)
    ########################################################################
    - name: Definir nombres de ISOs
      ansible.builtin.set_fact:
        # nombre del seed iso
        seed_iso_filename: "seed-{{ seed_vm_name }}.iso"
        # rutas completas en el datastore
        ubuntu_iso_path: "[{{ vcenter_datastore }}] {{ datastore_iso_dir }}/{{ ubuntu_iso_filename }}"
        seed_iso_path: "[{{ vcenter_datastore }}] {{ datastore_iso_dir }}/seed-{{ seed_vm_name }}.iso"

    - name: DEBUG nombres/rutas de ISOs
      ansible.builtin.debug:
        msg:
          ubuntu_iso_path: "{{ ubuntu_iso_path }}"
          seed_iso_path: "{{ seed_iso_path }}"

    ########################################################################
    # 5) Validaciones básicas
    ########################################################################
    - name: Validar credenciales y endpoint de vCenter
      ansible.builtin.assert:
        that:
          - vcenter_hostname | length > 0
          - vcenter_username | length > 0
          - (vcenter_password | default('')) | length > 0
        fail_msg: "Faltan parámetros de conexión a vCenter."
        success_msg: "Parámetros de vCenter OK."

    - name: Validar DC/Datastore/Cluster/Network
      ansible.builtin.assert:
        that:
          - vcenter_datacenter | length > 0
          - vcenter_datastore | length > 0
          - vcenter_cluster | length > 0
          - vcenter_network | length > 0
        fail_msg: "Faltan vcenter_datacenter / vcenter_datastore / vcenter_cluster / vcenter_network."
        success_msg: "Scope DC/DS/Cluster/Network OK."

    ########################################################################
    # 6) Borrar VM previa (si existe)
    ########################################################################
    - name: Eliminar VM seed previa (si existe)
      community.vmware.vmware_guest:
        hostname: "{{ vcenter_hostname }}"
        username: "{{ vcenter_username }}"
        password: "{{ vcenter_password }}"
        validate_certs: "{{ vcenter_validate_certs }}"
        datacenter: "{{ vcenter_datacenter }}"
        name: "{{ seed_vm_name }}"
        state: absent
        force: true
      ignore_errors: true

  tasks:
    ########################################################################
    # 7) Crear VM seed con ISOs montados
    ########################################################################
    - name: Crear VM base para autoinstall con ISOs
      community.vmware.vmware_guest:
        hostname: "{{ vcenter_hostname }}"
        username: "{{ vcenter_username }}"
        password: "{{ vcenter_password }}"
        validate_certs: "{{ vcenter_validate_certs }}"
        datacenter: "{{ vcenter_datacenter }}"
        cluster: "{{ vcenter_cluster }}"
        datastore: "{{ vcenter_datastore }}"
        folder: "/{{ vcenter_datacenter }}/vm"
        name: "{{ seed_vm_name }}"
        state: present
        guest_id: "ubuntu64Guest"
        hardware:
          num_cpus: "{{ seed_vm_num_cpus | int }}"
          memory_mb: "{{ seed_vm_memory_mb | int }}"
        disk:
          - size_gb: "{{ seed_vm_disk_gb | int }}"
            type: thin
        networks:
          - name: "{{ vcenter_network }}"
            type: dhcp
            typev6: dhcp
        cdrom:
          - type: iso
            iso_path: "{{ ubuntu_iso_path }}"
            controller_type: ide
            controller_number: 0
            unit_number: 0
            state: present
          - type: iso
            iso_path: "{{ seed_iso_path }}"
            controller_type: ide
            controller_number: 0
            unit_number: 1
            state: present
        wait_for_ip_address: false
      register: seed_create

    - name: Mostrar resultado de creación de la VM seed
      ansible.builtin.debug:
        var: seed_create