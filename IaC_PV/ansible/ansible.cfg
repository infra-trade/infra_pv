[defaults]
inventory = ansible/inventories/vcenter/hosts.yml
host_key_checking = False
retry_files_enabled = False

# Si luego quieres usar un archivo para la clave de Vault, puedes descomentar esta línea
# y poner SOLO la ruta SIN comentarios:
# vault_password_file = /home/pvinfra/.ansible_vault_pass

[privilege_escalation]
become = False
become_method = sudo
become_ask_pass = False